name: ubuntu-build

on:
  workflow_dispatch:
  push:
    branches: [ main, trunk, movement, boost_conversion, game_refine, handler_refine, phase_5, phase_6, data_setup, scripting, eluna, lfg_mechanics ]
    paths-ignore:
      - 'sql/**'
  pull_request:
    branches: [ main, trunk, movement, boost_conversion, game_refine, handler_refine, phase_5, phase_6, data_setup, scripting, eluna, lfg_mechanics ]
    paths-ignore:
      - 'sql/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

env:
  BUILD_TYPE: Release
  BOOST_VERSION: 1.91.0
  OPENSSL_VERSION: 4.0.0
  DEPS_DIR: ${{ github.workspace }}/.deps
  BOOST_ROOT: ${{ github.workspace }}/.deps/boost_1_91_0
  OPENSSL_ROOT_DIR: ${{ github.workspace }}/.deps/openssl-4.0.0
  BUILD_DIR: ${{ github.workspace }}/build/ubuntu
  ELUNA_BUILD_DIR: ${{ github.workspace }}/build/ubuntu-eluna
  ELUNA_INSTALL_DIR: ${{ github.workspace }}/build/ubuntu-eluna-install
  CCACHE_DIR: ${{ github.workspace }}/.ccache
  CC: gcc-14
  CXX: g++-14

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 360

    steps:
    - uses: actions/checkout@v4

    - name: Check game source tabs
      shell: pwsh
      run: ./tools/dev/game_metrics.ps1 -CheckNoTabs

    - name: Install Ubuntu dependencies
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          ca-certificates \
          ccache \
          cmake \
          git \
          libbz2-dev \
          libreadline-dev \
          ninja-build \
          perl \
          pkg-config \
          wget \
          zlib1g-dev

        sudo apt-get install -y default-libmysqlclient-dev || sudo apt-get install -y libmysqlclient-dev

    - name: Verify compiler toolchain
      shell: bash
      run: |
        set -euo pipefail
        command -v "$CC"
        command -v "$CXX"
        "$CC" --version
        "$CXX" --version

    - name: Cache Boost
      id: boost-cache
      uses: actions/cache@v4
      with:
        path: ${{ env.BOOST_ROOT }}
        key: ubuntu-boost-${{ env.BOOST_VERSION }}

    - name: Install Boost
      if: steps.boost-cache.outputs.cache-hit != 'true'
      shell: bash
      run: |
        set -euo pipefail
        mkdir -p "$DEPS_DIR"
        boost_archive="$RUNNER_TEMP/boost_1_91_0.tar.gz"
        wget -q "https://archives.boost.io/release/$BOOST_VERSION/source/boost_1_91_0.tar.gz" -O "$boost_archive"
        tar -xzf "$boost_archive" -C "$RUNNER_TEMP"
        cd "$RUNNER_TEMP/boost_1_91_0"
        ./bootstrap.sh
        ./b2 install --prefix="$BOOST_ROOT" --with-headers -j"$(nproc)"

    - name: Cache OpenSSL
      id: openssl-cache
      uses: actions/cache@v4
      with:
        path: ${{ env.OPENSSL_ROOT_DIR }}
        key: ubuntu-openssl-${{ env.OPENSSL_VERSION }}

    - name: Install OpenSSL
      if: steps.openssl-cache.outputs.cache-hit != 'true'
      shell: bash
      run: |
        set -euo pipefail
        mkdir -p "$DEPS_DIR"
        openssl_archive="$RUNNER_TEMP/openssl-$OPENSSL_VERSION.tar.gz"
        wget -q "https://www.openssl.org/source/openssl-$OPENSSL_VERSION.tar.gz" -O "$openssl_archive"
        tar -xzf "$openssl_archive" -C "$RUNNER_TEMP"
        cd "$RUNNER_TEMP/openssl-$OPENSSL_VERSION"
        ./Configure linux-x86_64 \
          --prefix="$OPENSSL_ROOT_DIR" \
          --openssldir="$OPENSSL_ROOT_DIR/ssl" \
          shared \
          enable-legacy
        make -j"$(nproc)"
        make install_sw install_ssldirs

    - name: Export dependency paths
      shell: bash
      run: |
        echo "$OPENSSL_ROOT_DIR/bin" >> "$GITHUB_PATH"
        echo "LD_LIBRARY_PATH=$OPENSSL_ROOT_DIR/lib64:$OPENSSL_ROOT_DIR/lib:${LD_LIBRARY_PATH:-}" >> "$GITHUB_ENV"

    - name: Cache ccache
      uses: actions/cache@v4
      with:
        path: ${{ env.CCACHE_DIR }}
        key: ubuntu-ccache-${{ github.ref_name }}-${{ github.sha }}
        restore-keys: |
          ubuntu-ccache-${{ github.ref_name }}-
          ubuntu-ccache-

    - name: Configure with PCH
      shell: bash
      run: |
        set -euo pipefail
        cmake -S "$GITHUB_WORKSPACE" -B "$BUILD_DIR" -G Ninja \
          -DCMAKE_BUILD_TYPE="$BUILD_TYPE" \
          -DCMAKE_C_COMPILER="$CC" \
          -DCMAKE_CXX_COMPILER="$CXX" \
          -DCMAKE_C_COMPILER_LAUNCHER=ccache \
          -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
          -DTOOLS=ON \
          -DPACKET_LOG_SERVER=ON \
          -DUSE_COREPCH=ON \
          -DUSE_SCRIPTPCH=ON \
          -DSKYFIRE_BUILD_TESTS=OFF \
          -DBOOST_ROOT="$BOOST_ROOT" \
          -DOPENSSL_ROOT_DIR="$OPENSSL_ROOT_DIR"

    - name: Build with PCH
      shell: bash
      run: |
        set -euo pipefail
        cmake --build "$BUILD_DIR" --parallel "$(nproc)"

    - name: Configure Eluna smoke
      shell: bash
      run: |
        set -euo pipefail
        cmake -S "$GITHUB_WORKSPACE" -B "$ELUNA_BUILD_DIR" -G Ninja \
          -DCMAKE_BUILD_TYPE="$BUILD_TYPE" \
          -DCMAKE_C_COMPILER="$CC" \
          -DCMAKE_CXX_COMPILER="$CXX" \
          -DCMAKE_C_COMPILER_LAUNCHER=ccache \
          -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
          -DCMAKE_INSTALL_PREFIX="$ELUNA_INSTALL_DIR" \
          -DCONF_DIR="$ELUNA_INSTALL_DIR/etc" \
          -DTOOLS=OFF \
          -DPACKET_LOG_SERVER=OFF \
          -DUSE_COREPCH=ON \
          -DUSE_SCRIPTPCH=ON \
          -DSKYFIRE_BUILD_TESTS=OFF \
          -DELUNA=ON \
          -DBOOST_ROOT="$BOOST_ROOT" \
          -DOPENSSL_ROOT_DIR="$OPENSSL_ROOT_DIR"

    - name: Build Eluna smoke
      shell: bash
      run: |
        set -euo pipefail
        cmake --build "$ELUNA_BUILD_DIR" --target authserver worldserver --parallel "$(nproc)"

    - name: Install Eluna smoke
      shell: bash
      run: |
        set -euo pipefail
        cmake --install "$ELUNA_BUILD_DIR"
        test -f "$ELUNA_INSTALL_DIR/etc/lua_scripts/startup_smoke.lua"
        test -d "$ELUNA_INSTALL_DIR/etc/lua_scripts/extensions"

    - name: ccache stats
      if: always()
      shell: bash
      run: ccache --show-stats || true

    - name: Upload CMake diagnostics
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: ubuntu-cmake-diagnostics
        path: |
          ${{ env.BUILD_DIR }}/CMakeCache.txt
          ${{ env.BUILD_DIR }}/CMakeFiles/CMake*.log
          ${{ env.ELUNA_BUILD_DIR }}/CMakeCache.txt
          ${{ env.ELUNA_BUILD_DIR }}/CMakeFiles/CMake*.log
